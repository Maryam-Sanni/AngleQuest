import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MainFooter = () => {
  return (
    <View style={{ paddingVertical: 40, paddingHorizontal: 140 }}>
      <Text>MainFooter</Text>

    </View>
  );
};

export default MainFooter;

const styles = StyleSheet.create({});
